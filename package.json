{
  "name": "backend-frontend",
  "version": "0.1.0",
  "description": "This collection package integrates the server and client for deployment.",
  "main": "server/app.js",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client && npm install --only=dev --prefix client && npm run build --prefix client --include dev"
  },
  "engines": {
    "npm": "8.x",
    "node": "16.x"
  },
  "dependencies": {
    "mongodb": "^4.10.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^8.25.0",
    "eslint-plugin-vue": "^9.6.0"
  }
}
